# Local development template. Do not commit credentials here: use DB_URL,
# DB_USERNAME, and DB_PASSWORD environment variables in every environment.
# db.url=jdbc:mysql://localhost:3306/computer_store?useSSL=true&serverTimezone=UTC&characterEncoding=UTF-8
# db.username=computer_store
# db.password=replace-with-a-local-secret
